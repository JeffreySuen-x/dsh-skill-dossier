# Task: 索引串行化与原子替换

- id: 20260903-2
- 日期: 2026-09-03
- session id: Codex Desktop thread（DSH session id 不适用）
- fixed point: 25f3ba830dd51276e3ab33f52e40d86ddbee6552

## Brief（要做什么）

把 `.dsh/skill-manager/index.json` 的所有读改写收敛到一个独立 store：同一工作区内串行执行，写入采用同目录临时文件 + 原子替换。只有文件不存在时返回空索引；损坏 JSON、权限错误和不合法根结构必须显式失败，不得被空库覆盖。

## 断言清单

- [x] 并发更新同一 cwd 时不丢失任一修改。
- [x] 不同 cwd 之间不共享锁或数据。
- [x] ENOENT 返回空索引；损坏 JSON、非对象根、权限/读取错误显式抛出。
- [x] 写入失败时原 index 不被直接覆盖，临时文件做尽力清理。
- [x] archive、record_eval、usage、uninstall/reinstall/deleteTrash、setOrigin 全部通过统一 store 修改。
- [x] POSIX 与 Windows 都生成同目录、字面路径的原子替换命令。
- [x] 完整测试、typecheck、build、pack 与双轴 review 通过。

## 公开测试缝

- `createIndexStore(storage)` 的 `read/update` 契约。
- `parseArchiveIndex(text)` 的受控失败契约。
- `atomicReplaceCommand(src, dst, isWindows)` 跨平台命令契约。
- 现有 manager HTTP/tool 入口（行为不改名）。

## 边界（不做什么）

- 不修改临时 skill 的会话隔离。
- 不改变停用/重装的业务契约，仅防止 index 并发丢写。
- 不迁移当前 DSH profile，不重启 DSH。

## 证据

| 轮次 | 命令 | 退出码 | 关键输出 | 判定 |
|---|---|---:|---|---|
| Red-1 | `pnpm exec vitest run tests/index-store.spec.ts` | 1 | `Cannot find module ../src/index-store.ts` | 确认独立 store 尚未存在 |
| Green-1 | `pnpm exec vitest run tests/index-store.spec.ts` | 0 | 5/5 passed | 并发串行、cwd 隔离、受控失败与原写入保留契约通过 |
| Red-2 | `pnpm exec vitest run tests/windows.spec.ts` | 1 | `atomicReplaceCommand is not a function` | 确认跨平台替换缝缺失 |
| Green-2 | `pnpm exec vitest run tests/windows.spec.ts` | 0 | 12/12 passed | POSIX `mv -f --` 与 Windows `.NET File.Move(..., true)` 契约通过 |
| Integration | `pnpm exec vitest run tests/plugin.integration.spec.ts tests/index-store.spec.ts tests/windows.spec.ts` | 0 | 27/27 passed | 坏索引可见、临时写入/原子替换/失败清理/拒绝操作不回写通过 |
| Full | `pnpm test` | 0 | 9 files / 70 tests passed | 全量回归通过 |
| Gate | `node qa/gates.mjs` | 0 | `PASS=4 FAIL=0` | test / typecheck / build / pack 通过 |
| Review-1 | Standards + Spec 双轴审查 `25f3ba8..502358a` | 1 | Spec 发现 cwd 别名可绕过锁；两轴发现 Windows cleanup 非字面路径和 fixed point 误记 | 已修正，待复核 |
| Probe | Darwin 25.6 临时目录实跑 `/bin/mv -f --` 与 `/bin/rm -rf --` | 0 | 文件正确移动/删除 | 否定“BSD 不支持 end-of-options `--`”的误报 |
| Red-3 | `pnpm exec vitest run tests/index-store.spec.ts tests/windows.spec.ts` | 1 | cwd `/a` vs `/a/.` 丢失 beta；`removeFileCommand` 缺失 | 别名锁与字面清理测试命中 |
| Green-3 | `pnpm exec vitest run tests/index-store.spec.ts tests/windows.spec.ts tests/plugin.integration.spec.ts` | 0 | 29/29 passed | physical lock key + `LiteralPath`/`rm -f` 清理通过 |
| Review-2 | Standards + Spec 复核 `25f3ba8..d2988c3` | 0 | P0=0，P1=0，P2=0；Standards 撤回 Darwin `--` 误报 | 双轴 PASS |

Lint：N/A（仓库没有 lint script 或配置）。

## Review 结论

- Standards: 最终 P0/P1/P2 均无；physical lock key、LiteralPath cleanup、fixed point 均通过复核。
- Spec: 最终 P0/P1/P2 均无；任务卡全部断言有实现与测试证据。

## 裁决

- [x] PASS
- [ ] 带债通过（债务：）
- [ ] 停止（原因：）
