# Task: 三板块收口 · 索引写路径 P0 · 汇报 skill→agent 打通

- id: 20260911-1
- 日期: 2026-09-11
- session id: DSH Web（本工作区会话）
- fixed point: ecbd2760cd09f842f5dbf9508949260c291b5427

## Brief（要做什么）

1. 回看整个项目，找出真实代码问题；
2. 聚焦「技能 / 档案 / 汇报」三板块，剪除低效率板块；
3. 把汇报的 skill→agent 链路打通（运行记录 / 结构化产物 / 定时 / 回写 / 分派）；
4. 提交前准备（改名、文档、发现机制）。

## 断言清单

- [x] P0：根因定位并复现「索引写路径被 fs 沙箱拒绝」——`indexStore.writeAtomic` 调 `fs.writeText` 时未传 sandbox policy，落到部署回退根（`process.cwd()`），跨工作区会话不在其下，被 `FS_SANDBOX_DENIED` 拒绝；`recordSkillUse` 的 `.catch(() => {})` 让它静默了 9 天。修法：索引写带上以工作区为根的 workspace-write 策略。
- [x] 该失败有红→绿回归测试（去掉修复即复现同一条生产错误信息）。
- [x] 埋点失败不再静默：`usageHealth` 经 RPC 暴露，面板顶部提示。
- [x] 面板 5 tab → 3 tab（技能/档案/汇报）；删 `skill_match`/`skill_route`/`skill_usage`/`skill_eval` 四个工具与 `src/match.ts`。
- [x] 档案增「实测成败」（来自 `tools/result`，非模型自述）与「目录 token 成本」。
- [x] 汇报：复盘按名调用 skill（保留内联回退）、以产物落盘为完成判据、结构化 JSON 契约 + 校验 + md 回退、停止跟踪、复盘历史、回写 brief、可选定时与子代理分派。
- [x] 汇报契约参数化（`Config.report`，默认值 = 旧行为）。
- [x] 四闸通过（test/typecheck/build/pack），`lib/` 已重建。
- [ ] 真机取证：重启 DSH 后确认写路径落盘、usage 计数增长、复盘运行状态可见（重启会中断当前 GUI 会话，留给用户择时执行）。

## 边界（不做什么）

- 不做 embedding / LLM-judge 路由（本机实测与同类工具一致：目录进系统提示即可）。
- 不自造 SSE/WebSocket；不自造 job 状态机（用产物落盘 + DSH 既有事件）。
- 不把「停止跟踪」包装成「中断」。
- 不把档案搬进 SKILL.md frontmatter（双写源，本轮不做）。
- 不重启当前 profile、不 push。

## 证据

| 轮次 | 命令 | 退出码 | 关键输出 | 判定 |
|---|---|---:|---|---|
| 1 | `curl -XPOST /api/skill-manager setOrigin` | 500 | `FsError: cannot write "…/.index.json.<pid>-<uuid>.tmp": file access denied under workspace-write mode` | P0 复现 |
| 2 | `curl -XPOST /api/report export` | 200 | 写出 `reporter/export/report-daily-2026-09-11.{md,json}` | 会话策略写成功，确认判别条件 |
| 3 | `vitest -t "workspace-rooted policy"`（临时回退修复） | 1 | 同一条 500 错误 | 红 |
| 4 | `vitest run` | 0 | 95 passed | 绿 |
| 5 | `node qa/gates.mjs`（4 闸） | 0 | PASS=4 | 绿 |
| 6 | `node scripts/pack-smoke.mjs` | 0 | `pack smoke PASS: dsh-skill-dossier-0.2.0.tgz` | 绿 |

## 结论

P0 已修复并有回归保护；三板块收口完成；汇报链路打通。剩余：真机重启取证 + 仓库改名/发布（需用户侧 GitHub 操作）。
