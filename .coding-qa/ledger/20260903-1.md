# Task: 管理与汇报收敛为单一可部署插件

- id: 20260903-1
- 日期: 2026-09-03
- session id: Codex Desktop thread（DSH session id 不适用）
- fixed point: 251802beb529ea2231b1fb78ea375bb78e4b4375

## Brief（要做什么）

把当前由 `dsh-skill-manager` client/host 与独立 `dsh-report` host 手工拼装的链路，收敛到 `dsh-skill-manager` 一个安装包。保留 `/api/report` 与现有 UI 契约，让全新安装管理插件时不再要求 profile 额外 link/insert 第二个包。

## 断言清单（完成标准，逐条可客观判定真伪）

- [x] 单独调用 package 导出的 `apply(ctx)` 会同时注册 `/api/skill-manager` 与 `/api/report`。
- [x] `/api/report` 能通过现有 brief 契约生成日报/月度，并拒绝跨站请求。
- [x] 汇报 UI 在 report 后端失败时显示结构化错误，而不是伪装成空数据。
- [x] npm tarball 不依赖 `dsh-report` package，安装到临时目录后可成功导入主入口，且包含运行所需入口、client、类型、README、LICENSE 与 patch。
- [x] README 明确单包安装、内部汇报能力与旧独立 report 的迁移关系。
- [ ] 相关单测、完整测试、typecheck、build、pack smoke 与工程闸门全部通过。
- [ ] 双轴 review 没有未处理的 P0/P1 finding。

## 公开测试缝

- package 主入口 `apply(ctx)`。
- `/api/skill-manager` 与 `/api/report` HTTP 路由。
- `npm pack` 生成的 tarball 在全新临时目录中的安装与主入口导入。

## 边界（不做什么）

- 不修改 `~/.dsh/profiles/web`，不重启当前 DSH。
- 不删除旧 `自创项目/dsh-report/`；本轮只让新管理包不再依赖它。
- 不顺手修索引原子事务、临时 skill 会话隔离或生命周期跨平台问题；这些在完成 P0 后重新排序。
- 不决定新的正式仓库名或产品名。

## 证据（逐轮追加）

| 轮次 | 命令 | 退出码 | 关键输出 | 判定 |
|---|---|---:|---|---|
| Red-1 | `pnpm exec vitest run tests/plugin.integration.spec.ts` | 1 | 预期两条路由，实际仅 `/api/skill-manager` | 确认单包激活契约尚未实现 |
| Green-1 | `pnpm exec vitest run tests/plugin.integration.spec.ts` | 0 | 首个单包激活测试通过 | 报表 host 已内聚 |
| Red-2 | `node scripts/pack-smoke.mjs` | 1 | `MODULE_NOT_FOUND` | 确认 tarball 安装缝缺失 |
| Green-2 | `pnpm run pack:smoke` | 0 | tarball 临时安装后注册两条 API | 新鲜安装契约通过 |
| Red-3 | `pnpm exec vitest run tests/plugin.integration.spec.ts` | 1 | brief 目录不存在时返回 `lastError` | 首次使用不应被当作故障 |
| Green-3 | `pnpm exec vitest run tests/plugin.integration.spec.ts tests/report.spec.ts` | 0 | 2 files / 8 tests passed | 同时验证单包、同源保护、空目录与解析契约 |
| Check | `pnpm run typecheck` | 0 | 无 TypeScript 错误 | 通过 |
| Check | 连续两次 `pnpm run build` 并比较三个 bundle 哈希 | 0 | `index.js` / `client.js` / map 哈希相同 | 构建可重复 |

Lint：N/A（仓库没有 lint script 或 lint 配置）。最终全量测试、工程闸门与 review 证据在后续轮次追加。

## Review 结论

- Standards: 待执行
- Spec: 待执行

## 裁决

- [ ] PASS
- [ ] 带债通过（债务：）
- [ ] 停止（原因：）
