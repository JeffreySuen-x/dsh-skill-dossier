# Task: 生命周期跨平台一致性

- id: 20260903-3
- 日期: 2026-09-03
- session id: Codex Desktop thread（DSH session id 不适用）
- fixed point: bf37c9a8af8601d57dd1238519296f4370c4fade

## Brief（要做什么）

统一文件系统 skill 的停用、重装和删除语义：Windows 始终使用字面路径；POSIX 目标冲突必须失败，不得把 `mv -n` 未移动当成成功；停用/重装在 index 原子提交失败时反向移动回滚。

## 断言清单

- [x] Windows mkdir/move/remove 都不解释 `[]*?` 等通配符。
- [x] POSIX move 在目标已存在时非零退出，且不覆盖原目标。
- [x] 停用在“目录已移入 trash，index 提交失败”时把目录移回原位。
- [x] 重装在“目录已恢复，index 提交失败”时把目录退回 trash。
- [x] 回滚失败时同时报告原错误与回滚错误，不伪装成功。
- [ ] 完整测试、typecheck、build、pack 与双轴 review 通过（四门禁已通过，review 待执行）。

## 公开测试缝

- `mkdirCommand` / `moveNoClobberCommand` / `removeRecursiveCommand` 纯函数。
- `/api/skill-manager` 的 `uninstall` / `reinstall` 路由。

## 边界（不做什么）

- 不修改临时 skill 会话隔离。
- 不改变 trash 目录位置、索引 schema 或 UI 文案。
- 不迁移当前 DSH profile，不重启 DSH。

## 证据

| 轮次 | 命令 | 退出码 | 关键输出 | 判定 |
|---|---|---:|---|---|
| Red | `pnpm exec vitest run tests/windows.spec.ts` | 1 | 13 项中 4 项失败：旧 Windows 命令仍使用 `-Path`，POSIX move 无冲突失败保证 | PASS（测试先行） |
| Green-聚焦 | `pnpm exec vitest run tests/plugin.integration.spec.ts tests/path.spec.ts tests/windows.spec.ts && pnpm typecheck` | 0 | 3 文件 / 36 测试通过；typecheck 通过 | PASS |
| Green-全量 | `pnpm test` | 0 | 9 文件 / 75 测试通过 | PASS |
| Gate | `node qa/gates.mjs` | 0 | test/typecheck/build/pack = 4 PASS / 0 FAIL | PASS |
| Review-Red-1 | `pnpm exec vitest run tests/path.spec.ts tests/index-store.spec.ts` | 1 | 18 项中 2 项失败：源缺失被误报成功；回滚不在串行区 | PASS（复现 review 发现） |
| Review-Red-2 | `pnpm exec vitest run tests/path.spec.ts tests/windows.spec.ts` | 1 | 25 项中 2 项失败：目标目录竞态；Windows 仍用容器语义 API | PASS（复现 review 发现） |
| Review-Green | `pnpm exec vitest run tests/plugin.integration.spec.ts tests/path.spec.ts tests/index-store.spec.ts tests/windows.spec.ts && pnpm typecheck` | 0 | 4 文件 / 45 测试通过；含真实目录回滚与竞态恢复 | PASS |
| Review-Gate | `node qa/gates.mjs && pnpm test` | 0 | 四门禁全通过；9 文件 / 78 测试通过 | PASS |

## Review 结论

- Standards（首轮）：1 P1 + 2 P2 + 1 P3；源缺失退出码、锁外回滚、目标容器 TOCTOU 已修复，真实文件回滚测试已补。终审待执行。
- Spec（首轮）：1 P1 + 1 P2，与 Standards 的前两项一致；已修复。终审待执行。

## 裁决

- [ ] PASS
- [ ] 带债通过（债务：）
- [ ] 停止（原因：）
