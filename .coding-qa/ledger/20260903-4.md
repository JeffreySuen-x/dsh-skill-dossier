# Task: 三系统 CI 与 Windows 生命周期 smoke

- id: 20260903-4
- 日期: 2026-09-03
- session id: Codex Desktop thread（DSH session id 不适用）
- fixed point: d19d7efc5fda8a5738edf11033810f224c8cf88c

## Brief（要做什么）

为建仓后的每次 push/PR 建立 macOS、Linux、Windows 质量矩阵，并让 Windows runner 真实执行 `MoveFileExW` 生命周期命令，填补当前只有命令生成断言的 P3 证据缺口。

## 断言清单

- [x] GitHub Actions 覆盖 `ubuntu-latest` / `macos-latest` / `windows-latest` 与 Node 22/24。
- [x] 每个矩阵节点用锁文件安装并运行 test/typecheck/build/pack 四闸。
- [x] Windows runner 已配置为真实验证目录和单文件往返移动、只读文件、目标冲突、源缺失和可用时的跨卷拒绝。
- [x] Windows 运行测试使用特殊字符路径，证明字面路径无通配符展开。
- [x] CI 契约测试、本机可运行部分、完整闸门与双轴 review 通过。

## 公开测试缝

- `.github/workflows/ci.yml` 的静态契约测试。
- `moveNoClobberCommand(..., true)` 在 Windows `pwsh` 中的真实文件系统测试。

## 边界（不做什么）

- 不创建 remote，不 push，不伪造 CI 已经实跑的结论。
- 不修改当前 DSH profile，不重启 DSH。
- 不处理临时 skill 会话隔离。

## 证据

| 轮次 | 命令 | 退出码 | 关键输出 | 判定 |
|---|---|---:|---|---|
| Red | `pnpm exec vitest run tests/ci.spec.ts` | 1 | `.github/workflows/ci.yml` ENOENT，1 failed | 证明 CI 契约在实现前会失败 |
| Green（聚焦） | `pnpm exec vitest run tests/ci.spec.ts tests/windows-runtime.spec.ts tests/windows.spec.ts && pnpm typecheck` | 0 | macOS 上 14 passed / Windows-only 6 skipped；typecheck PASS | 本机可运行部分通过 |
| Green（集成测试跨平台化） | `pnpm exec vitest run tests/plugin.integration.spec.ts tests/ci.spec.ts tests/windows-runtime.spec.ts && pnpm typecheck` | 0 | 14 passed / Windows-only 6 skipped；生命周期回滚集成测试已改为运行平台原生命令 | 防止 Windows 矩阵被 POSIX 断言误杀 |
| Green（四闸，修正前） | `node qa/gates.mjs && pnpm test && git diff --check && git diff --exit-code -- lib` | 0 | GATES PASS=4 FAIL=0；81 passed / 6 skipped | 后续需在最终差异上复跑 |
| Review 修正 | 聚焦测试 + 双轴审查 | 0 | 修复 Windows mock 的 POSIX 路径假设；`pnpm/setup` 禁用提前安装；枚举异卷且无异卷时显式 skip | 关闭 Windows 矩阵 P1 和两项 P2 |
| Red（构建可复现性） | `pnpm exec vitest run tests/build-reproducibility.spec.ts` | 1 | 已提交 `lib/client.js` 泄漏 `<checkout>/...`，CSS module identity 依赖 checkout 绝对路径 | 复现 CI 构建后 artifact diff 必红 |
| Green（跨目录重建） | 在 `/private/tmp/dsh-skill-manager-repro.*` 用同一编译器重建后 `cmp` + `shasum -a 256` | 0 | 原目录与临时目录的 `client.js` 均为 `13fdd7be...`，map 均为 `3a200e71...` | 跨 checkout 逐字节一致 |
| Green（最终差异） | `node qa/gates.mjs && pnpm test && git diff --check && git diff --exit-code -- lib` | 0 | GATES PASS=4 FAIL=0；11 files passed / 1 Windows-only file skipped；82 passed / 6 skipped；`lib/` 无漂移 | 本机质量闸通过；Windows 真实运行仍待 push 后 CI 取证 |

## Review 结论

- Standards: PASS，`ecdfd1d` 无 P0/P1/P2/P3；确认 Windows 分隔符、跨盘路径拒绝、跨 checkout 构建及 CI 全局契约。
- Spec: PASS，`ecdfd1d` 无 P0/P1/P2/P3；独立 `/private/tmp` 从提交快照重建后 `client.js` 与 map 逐字节一致。

## 裁决

- [x] PASS
- [ ] 带债通过（债务：）
- [ ] 停止（原因：）
