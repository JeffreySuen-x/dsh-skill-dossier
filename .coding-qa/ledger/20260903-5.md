# Task: 临时 skill 会话隔离

- id: 20260903-5
- 日期: 2026-09-03
- session id: Codex Desktop thread（DSH session id 不适用）
- fixed point: c1a1332b6154f0357d552452f628b838181d4d00

## Brief（要做什么）

把浏览器创建的临时 skill 从插件全局层改为 DSH agent/session scope，使两个会话可同时持有同名临时 skill，且互不可见、互不可卸载。

## 断言清单

- [x] 注册必须提供可解析的活跃 `sessionId`，不再回退到全局层。
- [x] 临时 skill 通过 `agent.ctx.get('skills').register(...)` 进入 agent scope。
- [x] 所有权按 session + name 分区；B 会话不能卸载 A 会话的 skill。
- [x] 两个会话可用不同内容注册同名 skill，各自 list/get 只看到自己的定义。
- [x] agent scope 销毁时清理所有权账本，插件卸载时处置仍存活的注册。
- [x] 聚焦测试、四闸、双轴 review 通过。

## 边界（不做什么）

- 不改动文件系统 skill 的全局/项目发现层级。
- 不修改 DSH 上游 scope 实现。
- 不创建 remote，不 push，不重启当前 profile。

## 证据

| 轮次 | 命令 | 退出码 | 关键输出 | 判定 |
|---|---|---:|---|---|
| Red | `pnpm exec vitest run tests/plugin.integration.spec.ts` | 1 | 13 通过、2 失败：两会话同名内容串扰，无会话注册未被拒绝 | 旧实现的全局所有权问题可重现 |
| Review Red | `pnpm exec vitest run tests/plugin.integration.spec.ts` | 1 | 20 通过、3 失败：ownership effect 残留、inactive scope 排队请求、catalog 快照后同层抢占 | Standards 轴 3 个 P2 均可稳定重现 |
| Green | `pnpm exec vitest run tests/plugin.integration.spec.ts` | 0 | 23 个集成测试全通过 | 会话隔离、同名定义、并发替换、scope/插件回收、在途与排队竞态、同层抢占均有覆盖 |
| Full test | `pnpm test` | 0 | 11 files passed, 1 skipped; 92 tests passed, 6 Windows-only skipped | PASS |
| Typecheck | `pnpm typecheck` | 0 | `tsc --noEmit` | PASS |
| Build | `pnpm build` | 0 | host/client 构建完成 | PASS |
| Pack smoke | `pnpm pack:smoke` | 0 | tarball 安装后 `/api/report` 与 `/api/skill-manager` 均注册 | PASS |
| Diff check | `git diff --check` | 0 | 无空白错误 | PASS |

## Review 结论

- Standards: PASS（首轮 3 个 P2：ownership effect 残留、inactive scope 排队竞态、同层注册抢占；均已修复，最终无 P0-P3）
- Spec: PASS（最终无 P0-P3）

## 裁决

- [x] PASS
- [ ] 带债通过（债务：）
- [ ] 停止（原因：）
